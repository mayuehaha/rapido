主界面使用QtWebKit.

程序启动时显示等待页面，同时启动一个监听线程。

监听线程中广播自己的上线信息，然后进入监听状态。

根据接收到的数据包决定做何操作。

需要维护的数据：
  1. 在线用户列表（软件版本[区分IPMSG和FeiQ]、IP地址、名称等）
  2.

特点：
	1. 正常收发IpMSG和FeiQ的数据，可以考虑不支持FeiQ的扩展
	2. 实现自己的通讯协议，包括组织结构、用户登录、与专用服务端联系等等。